$('#show_url').html("<%= j (render 'form') %>");
$('#show_url').slideDown(350);
$('#url_url').val("<%= request.base_url + "/" + @url.slug %>");
$("#clear_url").show();
$("#copy_url").show();
$("#create_url").hide();